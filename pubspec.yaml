name: notably
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Dependencias existentes
  supabase_flutter: ^2.9.1
  flutter_riverpod: ^2.0.0
  
  # Rich Text Editor - NUEVAS
  flutter_quill: ^8.5.0
  flutter_quill_extensions: ^0.6.10
  
  # Updated dependencies for compatibility
  flutter_quill: 7.2.8
  flutter_quill_extensions: 0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true